class Solution {
    public String convertDateToBinary(String date) {
       String[] part=date.split("-");
       int year=Integer.ParseInt(part[0]); 
        int year=Integer.ParseInt(part[0]); 
    }
}